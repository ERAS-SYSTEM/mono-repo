{
  "name": "@repo/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:push": "drizzle-kit push",
    "build": "pnpm exec tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"require('fs').rmSync('drizzle',{recursive:true,force:true})\""
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "drizzle-orm": "^0.44.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.7.2"
  }
}

